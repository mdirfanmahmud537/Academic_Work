#include <stdio.h>
int main() {
    int n = 9875, sum;
    while (n >= 10) {
        sum = 0;
        while (n > 0) {
            sum += n % 10;
            n /= 10;
        }
        n = sum;
    }
    printf("Single digit sum: %d\n", n);
    return 0;
}
