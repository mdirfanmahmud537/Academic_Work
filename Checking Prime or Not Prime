#include <stdio.h>
int main() {
    int n, i, isPrime = 1;
    printf("Enter an integer: ");
    scanf("%d", &n);
    if (n <= 1) {
        isPrime = 0;
    } else {
        for (i = 2; i * i <= n; i++) {
            if (n % i == 0) {
                isPrime = 0;
                break;
            }
        }
    }
    if (isPrime) {
        printf("Prime number\n");
    } else {
        printf("Not prime Number\n");
    }
    return 0;
}
