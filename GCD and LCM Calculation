#include <stdio.h>
int main() {
    int a = 12, b = 18, gcd, lcm, i;
    for (i = 1; i <= a && i <= b; i++) {
        if (a % i == 0 && b % i == 0)
            gcd = i;
    }
    lcm = (a * b) / gcd;
    printf("GCD: %d\nLCM: %d\n", gcd, lcm);
    return 0;
}
